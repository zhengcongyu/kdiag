KDiag Web service:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "kdiag.fullname" . }}-web 8088:{{ .Values.service.webPort }}

Active probes are {{ ternary "enabled" "disabled (recommended default)" .Values.probeRunner.enabled }}.
Provide PostgreSQL through an existing Secret, then set api.databaseSecret.name and api.databaseSecret.key.
Do not put database credentials in Helm values or command-line arguments.
